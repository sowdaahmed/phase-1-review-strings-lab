// Define currentUser as a string containing a name
const currentUser = "Ayan"; // Replace with any name you want

// Create a welcome message that includes currentUser
const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

// Create an excited welcome message in uppercase
const excitedWelcomeMessage = `WELCOME TO FLATBOOK, ${currentUser.toUpperCase()}!`;

// Create a short greeting using the first initial of currentUser
const shortGreeting = `Welcome, ${currentUser[0]}!`;

